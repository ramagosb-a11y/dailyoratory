# UX-SPEC — <feature name>

Feature ID / revision / reviewer / date / verdict:

- Existing route and user entry point:
- Existing components/tokens to reuse:
- New components, only if needed, with reason:
- User flow and prayer pacing:
- Start / loading / empty / error / completion / return states:
- Mobile behavior and touch targets:
- Desktop behavior:
- Keyboard order, focus and semantic controls:
- Accessible names, descriptions and status announcements:
- Zoom, contrast, reading order and reduced motion:
- Clipboard/print/storage failure behavior where applicable:
- Content IDs and immutable approved text:
- Acceptance checks and remaining questions:

Handoff: exact behavior and reusable contracts, required checks, blockers, next owner.
